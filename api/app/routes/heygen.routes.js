module.exports = app => {
  // const heygen = require("../controllers/heygen.controller.js");

  // var router = require("express").Router();

  // router.get("/ping", heygen.ping);

  // Create a new Tutorial
  // router.post("/", heygen.create);

  // // Retrieve all Tutorials
  // router.get("/", heygen.findAll);

  // // Retrieve all published Tutorials
  // router.get("/published", heygen.findAllPublished);

  // // Retrieve a single Tutorial with id
  // router.get("/:id", heygen.findOne);

  // // Update a Tutorial with id
  // router.put("/:id", heygen.update);

  // // Delete a Tutorial with id
  // router.delete("/:id", heygen.delete);

  // // Delete all Tutorials
  // router.delete("/", heygen.deleteAll);

  // app.use('/api/heygen', router);
};
